# vla-agent: Rabo -> 4080 remote VLA/oracle runtime
setuptools>=68,<80
numpy==1.26.4
opencv-python-headless>=4.8,<5
PyYAML>=6,<7
requests>=2.31,<3
websocket-client>=1.8,<2

# Rabo platform private SDKs (installed/injected by the platform runtime)
rabo-dev-kit
rabo_robocap

# Keep the template LLM dependency available; the vla_agent itself does not use it.
openai>=1.0
